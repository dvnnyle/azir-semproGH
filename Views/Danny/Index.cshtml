@{
    ViewData["Title"] = "Danny var her";
}
<style>
    .danny-lab {
        margin: -1rem -0.75rem 0;
        padding: 0;
        min-height: calc(100vh - 4rem);
        background: #0d1520;
        color: #c0c5ce;
        font-family: 'Space Mono', monospace;
        display: flex;
        align-items: center;
        justify-content: center;
        text-align: center;
    }
    .danny-lab__title {
        margin: 0;
        font-size: clamp(32px, 10vw, 72px);
        font-weight: 700;
        color: #ff5555;
        letter-spacing: 3px;
    }
</style>
<div class="danny-lab">
    <h1 class="danny-lab__title">DANNYYYYYYYYYY TEST</h1>
</div>